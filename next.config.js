/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
        domains: [
            'd1fxy698ilbz6u.cloudfront.net'
        ]
    }
}

module.exports = nextConfig
